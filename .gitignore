/target
CLAUDE.md
tmp/

# Generated by benches/setup_redis_c_bench.sh (contains Redis-licensed code)
benches/redis_c/
benches/redis_c_comparison.rs
# Anchored: a bare `build.rs` also matched the committed
# experiments/proto-resp/build.rs, which made release-plz abort on every push
# to main with "the working directory of this project has uncommitted changes".
/build.rs

# Proptest writes failing seeds here. Regenerated on demand; the boundary
# cases that matter are pinned by deterministic tests instead.
tests/*.proptest-regressions
